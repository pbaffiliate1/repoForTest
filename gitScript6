<script>
<scriptACL>
<execute>all</execute>
<read>all</read>
<write>all</write>
</scriptACL>
<code> 
<![CDATA[	
systemLogger.setLevel("off");
/*
The code below will:
create a variable
will check if the status is null
and it will do nothing
*/
	  var status = request.parameters["status"];
	  //This is an if statement
	  if(status == null || status == ""){
		  status = "200";
	  }

	  /*This is the last comment*/
return "test the commented out content3";

]]>
</code>
</script>